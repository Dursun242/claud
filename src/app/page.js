'use client'

export default function Page() {
  return (
    <div style={{
      height: '100vh',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      background: '#0F172A',
      fontFamily: "'DM Sans', sans-serif",
      color: '#fff',
      fontSize: 24,
      fontWeight: 700,
    }}>
      ✅ Route / Fonctionne Sans Erreur 500
    </div>
  )
}
